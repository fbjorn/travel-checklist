export interface Point {
  x: number;
  y: number;
}

// export type CheckListCategory = { name: string, items: Array<string> };
// export type CheckList = Array<CheckListCategory>;
export interface CheckListCategory {
  name: string;
  items: Array<string>;
}
